import * as React from "react";
import banner from "../Crystal-Hunt.png";

const Header = (props: object) => (
  <div>
    <img src={banner} width="300" height="70" />
  </div>
);

export default Header;
